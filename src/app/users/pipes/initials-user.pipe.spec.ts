import { InitialsUserPipe } from './initials-user.pipe';

describe('InitialsUserPipe', () => {
  it('create an instance', () => {
    const pipe = new InitialsUserPipe();
    expect(pipe).toBeTruthy();
  });
});
